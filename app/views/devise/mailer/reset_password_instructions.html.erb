<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restablecer contraseña - Smash Seeding Chile</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      line-height: 1.6;
      color: #e2e8f0;
      background-color: #0f172a;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      text-align: center;
      padding: 40px 0;
      border-bottom: 1px solid #334155;
    }
    .logo {
      color: #ef4444;
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 10px;
    }
    .content {
      padding: 40px 0;
      text-align: center;
    }
    .title {
      color: #f1f5f9;
      font-size: 28px;
      font-weight: bold;
      margin-bottom: 20px;
    }
    .message {
      color: #94a3b8;
      font-size: 16px;
      margin-bottom: 30px;
      line-height: 1.6;
    }
    .button {
      display: inline-block;
      background-color: #ef4444;
      color: white;
      text-decoration: none;
      padding: 15px 30px;
      border-radius: 8px;
      font-weight: 600;
      font-size: 16px;
      margin: 20px 0;
      transition: background-color 0.2s;
    }
    .button:hover {
      background-color: #dc2626;
    }
    .footer {
      border-top: 1px solid #334155;
      padding: 30px 0;
      text-align: center;
      color: #64748b;
      font-size: 14px;
    }
    .link {
      color: #ef4444;
      text-decoration: none;
    }
    .link:hover {
      text-decoration: underline;
    }
    .warning {
      background-color: #451a03;
      border: 1px solid #92400e;
      border-radius: 8px;
      padding: 15px;
      margin: 20px 0;
      color: #fbbf24;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">🔥 Smash Seeding Chile</div>
      <p style="color: #64748b; margin: 0;">Plataforma de Seeding para Torneos</p>
    </div>
    
    <div class="content">
      <h1 class="title">Restablecer Contraseña</h1>
      
      <p class="message">
        Hola <strong><%= @resource.email %></strong>,<br><br>
        Recibimos una solicitud para restablecer la contraseña de tu cuenta en Smash Seeding Chile.
      </p>
      
      <%= link_to "Cambiar mi contraseña", edit_password_url(@resource, reset_password_token: @token), 
            class: "button", 
            style: "background-color: #ef4444; color: white; text-decoration: none; padding: 15px 30px; border-radius: 8px; font-weight: 600; font-size: 16px; display: inline-block;" %>
      
      <div class="warning">
        <strong>⚠️ Importante:</strong> Este enlace expirará en unas pocas horas por seguridad.
      </div>
      
      <p class="message">
        Si no solicitaste este cambio, puedes ignorar este correo y tu contraseña permanecerá sin cambios.
      </p>
      
      <p style="color: #64748b; font-size: 14px; margin-top: 30px;">
        Si no puedes hacer clic en el botón, copia y pega este enlace en tu navegador:<br>
        <a href="<%= edit_password_url(@resource, reset_password_token: @token) %>" class="link">
          <%= edit_password_url(@resource, reset_password_token: @token) %>
        </a>
      </p>
    </div>
    
    <div class="footer">
      <p>
        Este correo fue enviado desde <strong>Smash Seeding Chile</strong><br>
        Si no solicitaste este cambio, tu cuenta sigue siendo segura.
      </p>
      <p style="margin-top: 20px;">
        <a href="http://localhost:3000" class="link">Visitar la plataforma</a>
      </p>
    </div>
  </div>
</body>
</html>
