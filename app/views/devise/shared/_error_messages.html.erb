<% if resource.errors.any? %>
  <div class="bg-red-900/50 border border-red-700 text-red-200 px-4 py-3 rounded-md mb-6" role="alert">
    <div class="flex items-center mb-2">
      <svg class="h-5 w-5 mr-2 text-red-400" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
      </svg>
      <h3 class="text-sm font-medium">
        <%= pluralize(resource.errors.count, "error") %> impidió que se guardara este <%= resource.class.model_name.human.downcase %>:
      </h3>
    </div>
    <ul class="list-disc list-inside text-sm space-y-1">
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
