<div class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 py-8">
  <div class="container mx-auto px-4 max-w-2xl">
    <!-- Encabezado -->
    <div class="mb-6">
      <%= link_to user_player_requests_path, 
            class: "inline-flex items-center text-slate-400 hover:text-slate-100 transition-colors duration-200 mb-4" do %>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L4.414 9H17a1 1 0 110 2H4.414l5.293 5.293a1 1 0 010 1.414z" clip-rule="evenodd" />
        </svg>
        Volver a solicitudes
      <% end %>
      
      <h1 class="text-3xl font-bold text-slate-100 mb-2">Solicitar Vinculación</h1>
      <p class="text-slate-400">Busca tu perfil de jugador para vincular con tu cuenta.</p>
    </div>

    <!-- Formulario de solicitud -->
    <div class="bg-slate-800 rounded-lg shadow-xl border border-slate-700 p-6">
      <%= form_with model: @request, url: user_player_requests_path, 
            data: { controller: "player-search", turbo: false },
            class: "space-y-6" do |form| %>
        
        <!-- Búsqueda de jugador -->
        <div>
          <label class="block text-sm font-medium text-slate-200 mb-2">
            Buscar Jugador
          </label>
          
          <div class="relative">
            <input type="text" 
                   data-action="input->player-search#search"
                   data-player-search-target="searchInput"
                   placeholder="Escribe tu tag de jugador o nombre completo..."
                   class="w-full px-4 py-3 bg-slate-700 border border-slate-600 text-slate-100 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent placeholder-slate-400">
            
            <div class="absolute inset-y-0 right-0 flex items-center pr-3">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-slate-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
            </div>
          </div>

          <!-- Resultados de búsqueda -->
          <div data-player-search-target="results" 
               class="hidden mt-2 bg-slate-700 border border-slate-600 rounded-lg max-h-80 overflow-y-auto">
          </div>

          <!-- Jugador seleccionado -->
          <div data-player-search-target="selectedPlayer" class="hidden mt-4">
            <div class="p-4 bg-slate-700 border border-slate-600 rounded-lg">
              <div class="flex items-center justify-between">
                <div data-player-search-target="selectedPlayerInfo">
                  <!-- Info del jugador seleccionado se llenará por JS -->
                </div>
                <button type="button" 
                        data-action="click->player-search#clearSelection"
                        class="text-slate-400 hover:text-slate-100 transition-colors duration-200">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd" />
                  </svg>
                </button>
              </div>
            </div>
          </div>

          <!-- Campo oculto para el ID del jugador -->
          <%= form.hidden_field :player_id, data: { player_search_target: "playerIdField" } %>
        </div>

        <!-- Mensaje de solicitud -->
        <div>
          <%= form.label :message, "Mensaje para el administrador", 
                class: "block text-sm font-medium text-slate-200 mb-2" %>
          <%= form.text_area :message, 
                rows: 4,
                placeholder: "Explica brevemente por qué este jugador corresponde a tu identidad...",
                class: "w-full px-4 py-3 bg-slate-700 border border-slate-600 text-slate-100 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent placeholder-slate-400 resize-none" %>
          <p class="mt-1 text-xs text-slate-400">
            Máximo 500 caracteres. Este mensaje ayudará al administrador a verificar tu solicitud.
          </p>
        </div>

        <!-- Botones -->
        <div class="flex gap-3">
          <%= link_to user_player_requests_path, 
                class: "flex-1 inline-flex justify-center items-center px-4 py-3 bg-slate-600 hover:bg-slate-700 text-white font-medium rounded-lg transition-colors duration-200" do %>
            Cancelar
          <% end %>
          
          <%= form.submit "Enviar Solicitud", 
                data: { player_search_target: "submitButton" },
                disabled: true,
                class: "flex-1 inline-flex justify-center items-center px-4 py-3 bg-red-600 hover:bg-red-700 disabled:bg-slate-600 disabled:cursor-not-allowed text-white font-medium rounded-lg transition-colors duration-200" %>
        </div>
      <% end %>
    </div>

    <!-- Jugadores sugeridos -->
    <% if @suggested_players.any? %>
      <div class="mt-6 bg-slate-800 rounded-lg shadow-xl border border-slate-700 p-6">
        <h2 class="text-lg font-semibold text-slate-100 mb-4 flex items-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-yellow-500" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M3 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clip-rule="evenodd" />
          </svg>
          Jugadores Sugeridos
        </h2>
        
        <p class="text-slate-400 mb-4">
          Basado en tu información de usuario, estos podrían ser tus perfiles:
        </p>

        <div class="space-y-2" data-controller="player-search">
          <% @suggested_players.each do |player| %>
            <div class="p-3 bg-slate-700 hover:bg-slate-600 rounded-lg cursor-pointer transition-colors duration-200"
                 data-action="click->player-search#selectPlayer"
                 data-player-id="<%= player.id %>"
                 data-player-tag="<%= player.entrant_name || player.name %>"
                 data-player-name="<%= player.name || player.entrant_name %>">
              
              <div class="flex items-center justify-between">
                <div>
                  <h3 class="font-medium text-slate-100">
                    <%= player.entrant_name || 'Sin tag' %>
                    <% if player.name.present? && player.name != player.entrant_name %>
                      <span class="text-slate-400">(<%= player.name %>)</span>
                    <% end %>
                  </h3>
                  <p class="text-sm text-slate-400">
                    <%= pluralize(player.events.joins(:tournament).distinct.count, 'torneo') %> • 
                    <%= pluralize(player.events.count, 'evento') %>
                  </p>
                </div>
                
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-slate-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>

    <!-- Información adicional -->
    <div class="mt-6 bg-blue-900/20 border border-blue-800 rounded-lg p-4">
      <div class="flex">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-400 mr-3 flex-shrink-0 mt-0.5" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
        </svg>
        <div class="text-blue-200">
          <h3 class="font-medium mb-1">Importante</h3>
          <ul class="text-sm space-y-1">
            <li>• Solo puedes vincular UN jugador por cuenta</li>
            <li>• La vinculación requiere aprobación de un administrador</li>
            <li>• Asegúrate de seleccionar TU perfil de jugador</li>
            <li>• Una vez aprobada, podrás editar la información del jugador</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div> 